# 参考图 2 转录 · 主角像素素材规格表（最高优先级基准）

> 来源：会话附带参考图 2（AI 生成规格表，原 PNG 未能入库）。
> **补录步骤**：将原图放入 `assets/reference/02_character_sheet.png` 后 commit。
> 本文件为其完整文字转录；如与原图冲突，以原图为准。

## 主角设定
- 兜帽银白发 Q 版女主（二头身），蓝眼，深靛蓝兜帽斗篷 + 白色饰边，佩剑。
- 三视图：正面 / 侧面 / 背面。
- 配色参考（自左至右）：深靛 `#2A2A3E`、靛紫 `#4A4A6A`、灰白 `#C8C8D8`、紫 `#7A6AA8`、浅紫 `#9A8AC8`、蓝紫 `#6A5A9A`、亮青 `#8AB4F8`。

## 武器 / 装备（5 种，全部可做拾取与切换）
长剑 / 弓 / 法杖 / 匕首 / 魔法书。

## 动作清单（帧表行）
- **基础动作**：待机、行走、奔跑、跳跃（各约 5 帧）。
- **战斗动作**：普通攻击(剑)、蓄力攻击(剑)、连击、受击/硬直/倒地、远程攻击(弓)、箭雨(多段)、蓄力射击、闪避/翻滚（带残影）。
- **魔法技能**：火球术、冰霜术、雷电术、召唤术。
- **特殊动作**：攀爬、开门/互动、拾取/使用道具、死亡。

## 特效 / 细节
攻击特效（青色月牙）、命中特效（星芒）、魔法特效（法阵）、脚下光效、受伤特效（红色粒子）、粒子效果（蓝色碎晶）。

## 其他素材
头像、水晶、药水、金币、宝箱、卷轴。

## 帧率要求（用户硬性要求）
- 目标 **60 FPS 表现**：原始 AI 帧表为关键帧（4~6 pose/动作），引擎侧以
  固定 60Hz 模拟 + 帧间插值（位移/旋转/缩放 tween）+ 残影/粒子/光效层补足流畅度；
  `frames.json` 支持任意帧数，后续美术补画中间帧可无缝替换。
